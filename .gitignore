node_modules/
dist/
build/
coverage/
*.log

# Runtime data — never committed, never packaged.
# 必须锚定到仓库根：不加前导斜杠时 `media/` 也会吞掉 `packages/server/src/media/`
# 里的新文件（`src/media/variants.ts` 就是这样漏提交的，CI 才在别处报模块找不到）。
/data/
/media/
/backups/
/logs/
.env
.env.*
!.env.example
config/models.json
config/persona.json

# Server-owned private media assets — kept outside Git/CI.
/assets/stickers/
/assets/references/

# Test output
e2e/test-results/
playwright-report/
release/
*.tsbuildinfo
.DS_Store
test-results/
.worktrees/

# 根目录临时运维脚本(ssh/部署排查用),不进仓库
/*.py
